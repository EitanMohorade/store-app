# ── Store App — Variables de entorno ──────────────────
# Este archivo NO se sube al repositorio (.gitignore lo excluye).
# Modificá VITE_API_BASE_URL si tu backend corre en otro puerto.

VITE_API_BASE_URL=http://localhost:8080
